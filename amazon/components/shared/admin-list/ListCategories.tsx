import React from 'react';

interface Props {
  className?: string;
}

export const ListCategories: React.FC<Props> = ({ className }) => {
  return (
    <div className={className}>

    </div>
  );
};

export default ListCategories;
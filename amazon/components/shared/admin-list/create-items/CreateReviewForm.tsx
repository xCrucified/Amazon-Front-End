import React from 'react';

interface Props {
  className?: string;
}

export const CreateReviewForm: React.FC<Props> = ({ className }) => {
  return (
    <div className={className}>

    </div>
  );
};

export default CreateReviewForm;